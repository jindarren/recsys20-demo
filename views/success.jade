doctype html
html
    head
        title Spotify Visualization
        style.
            body{
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
            }

            p{
                font-size:200%;
            }

            img{
                width:100%;
            }

            #code{
                color: blue;
                background-color: #e1e100;
                font-weight: bold;
                margin: 5px;
            }

    body
        p You have finished the study. Thank you~
        div(style='width:40%;height:auto')
            img(src="https://media.giphy.com/media/osjgQPWRx3cac/giphy.gif")
        script(type = "text/javascript", src = "../jquery/dist/jquery.min.js", charset = 'utf-8')
        script.
            $.getJSON("/data/user_profile_with_preference_model_updated.json",function (data) {
                var profile = {}
                console.log(typeof(data))
                profile["user_profile"] = data
                console.log(profile)
                $.ajax({
                    type: "POST",
                    url: "/initialize_user_model",
                    data: JSON.stringify(profile),
                    success: function (result) {
                        console.log(result)
                    },
                    contentType:"application/json;charset=utf-8",
                    dataType: "json"
                });
            })

